{% extends 'base.html.twig' %}

{% block title %}LDO - Absence
{% endblock %}

{% block body %}
	<div class="container text-light">
		<h1 class="text-center m-5">
			<i class="fas fa-bed"></i>
			Absence :</h1>
		{{ form_start(formabsence) }}
		<div class="row">
			<div class="col">
				{{ form_row(formabsence.debut) }}
			</div>
		</div>
		<div class="row">
			<div class="col">
				{{ form_row(formabsence.fin) }}
			</div>
		</div>
		<div class="row">
			<div class="col text-center">
				<button type="submit" class="btn btn-primary btn-lg text-center">Enregistrer</button>
				{{ form_end(formabsence) }}
			</div>
		</div>
	{% endblock %}
